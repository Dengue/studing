function AutoCompete(defaultBase){	if(!defaultBase)		this.container = ['grapes', 'grapefruit', 'orange'];	else		this.container = defaultBase;}AutoCompete.prototype.add = function(val){	this.container.push(val);}AutoCompete.prototype.match = function(inputVal){	if(inputVal === '')		return [''];	var matches = [];	var regVal = new RegExp('^' + inputVal);	for(var i = 0,l = this.container.length; i < l; i++){		var rez = regVal.test(this.container[i]);		if(rez === true){			matches.push(this.container[i]);		}	}	if(matches.length === 0 ){		matches.push('');	}	return matches;}AutoCompete.prototype.remove = function(deleteVal){	var oldContainer = this.container;	this.container = [];	for(var i = 0,l = oldContainer.length; i < l; i++){		if(oldContainer[i] !== deleteVal){			this.container.push(oldContainer[i]);		}	}}